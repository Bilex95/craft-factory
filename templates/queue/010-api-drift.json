{
  "repoName": "api-drift",
  "description": "A node:test contract suite against a live public currency API — catches schema drift before your app does.",
  "topics": [
    "api-testing",
    "contract-testing",
    "node-test",
    "qa",
    "good-first-issues"
  ],
  "files": [
    {
      "path": "README.md",
      "content": "# api-drift\n\n**The problem:** your app depends on a third-party API. One day they rename a field, change a type, or start returning `null` where they used to return a number — and you find out from a production error, not a test.\n\n**The solution:** a small contract suite that hits the **live** API and asserts the shape your code relies on. Run it in CI on a schedule; when the provider drifts, the suite goes red before your users do.\n\nThe target here is [Frankfurter](https://www.frankfurter.app/) (free ECB exchange rates, no API key).\n\n## Run it\n\n```bash\nnpm test\n```\n\nNeeds Node 18+ (for built-in `fetch` and `node:test`). No dependencies, no install step.\n\nPoint it at a different host without touching code:\n\n```bash\nAPI_BASE=https://api.frankfurter.dev npm test\n```\n\n## How it's built\n\n- `lib/schema.js` — a ~30-line shape checker (`checkShape(value, spec)`); `spec` values are a `typeof` string, a `RegExp`, or a predicate function.\n- `test/contract.test.js` — five tests: the `/latest` shape, `from`/`to` filtering, the historical endpoint, error handling for an unknown currency, and a loose \"rates in a sane band\" drift guard.\n\n## Contribute\n\n- **Add a `/currencies` contract test** (see the open issue).\n- Add a test that fails loudly if a rate moves more than 20% from a checked-in snapshot (real drift detection).\n- Wrap `fetch` with a 1-retry-on-5xx helper and test it against `https://httpstat.us/503`.\n\n---\n\nScaffolded by an automated weekly pipeline, then refined by hand — see the factory repo for how it works.\n"
    },
    {
      "path": "lib/schema.js",
      "content": "// Tiny shape checker — enough to assert an API response hasn't drifted,\n// without pulling in a schema library.\n\n// spec is an object of { key: checker }. A checker is one of:\n//   \"string\" | \"number\" | \"boolean\" | \"object\"   (typeof check)\n//   a RegExp                                       (tested against String(value))\n//   a function (value) => boolean                  (custom predicate)\n// Returns an array of human-readable problems; empty means it matches.\nfunction checkShape(value, spec, path = \"\") {\n  const problems = [];\n  if (typeof value !== \"object\" || value === null) {\n    return [`${path || \"value\"} is not an object`];\n  }\n  for (const [key, checker] of Object.entries(spec)) {\n    const here = path ? `${path}.${key}` : key;\n    if (!(key in value)) {\n      problems.push(`missing key: ${here}`);\n      continue;\n    }\n    const v = value[key];\n    if (typeof checker === \"string\") {\n      if (typeof v !== checker) problems.push(`${here}: expected ${checker}, got ${typeof v}`);\n    } else if (checker instanceof RegExp) {\n      if (!checker.test(String(v))) problems.push(`${here}: ${JSON.stringify(v)} fails ${checker}`);\n    } else if (typeof checker === \"function\") {\n      if (!checker(v)) problems.push(`${here}: ${JSON.stringify(v)} fails predicate`);\n    }\n  }\n  return problems;\n}\n\nconst isCurrencyCode = (v) => /^[A-Z]{3}$/.test(v);\nconst isIsoDate = (v) => /^\\d{4}-\\d{2}-\\d{2}$/.test(v);\nconst isPositiveNumber = (v) => typeof v === \"number\" && Number.isFinite(v) && v > 0;\n\nmodule.exports = { checkShape, isCurrencyCode, isIsoDate, isPositiveNumber };\n"
    },
    {
      "path": "package.json",
      "content": "{\n  \"name\": \"api-drift\",\n  \"version\": \"1.0.0\",\n  \"private\": true,\n  \"description\": \"Contract tests for a live public currency API using node:test.\",\n  \"engines\": { \"node\": \">=18\" },\n  \"scripts\": {\n    \"test\": \"node --test\"\n  }\n}\n"
    },
    {
      "path": "test/contract.test.js",
      "content": "const test = require(\"node:test\");\nconst assert = require(\"node:assert/strict\");\nconst {\n  checkShape,\n  isCurrencyCode,\n  isIsoDate,\n  isPositiveNumber,\n} = require(\"../lib/schema\");\n\n// Frankfurter — free, no key, ECB reference rates. Override if the host moves.\nconst API_BASE = process.env.API_BASE || \"https://api.frankfurter.app\";\n\nasync function get(path) {\n  const res = await fetch(API_BASE + path);\n  let body = null;\n  try {\n    body = await res.json();\n  } catch {\n    /* leave body null for non-JSON error responses */\n  }\n  return { res, body };\n}\n\ntest(\"GET /latest matches the documented shape\", async () => {\n  const { res, body } = await get(\"/latest\");\n  assert.ok(res.ok, `expected 2xx, got ${res.status}`);\n\n  const problems = checkShape(body, {\n    amount: isPositiveNumber,\n    base: isCurrencyCode,\n    date: isIsoDate,\n    rates: (v) => typeof v === \"object\" && v !== null,\n  });\n  assert.deepEqual(problems, [], problems.join(\"; \"));\n\n  for (const [code, rate] of Object.entries(body.rates)) {\n    assert.ok(isCurrencyCode(code), `bad currency code: ${code}`);\n    assert.ok(isPositiveNumber(rate), `bad rate for ${code}: ${rate}`);\n  }\n});\n\ntest(\"GET /latest honours from/to and returns only the requested pair\", async () => {\n  const { res, body } = await get(\"/latest?from=USD&to=GBP\");\n  assert.ok(res.ok, `expected 2xx, got ${res.status}`);\n  assert.equal(body.base, \"USD\");\n  assert.deepEqual(Object.keys(body.rates), [\"GBP\"]);\n});\n\ntest(\"historical endpoint returns the exact day requested\", async () => {\n  const { res, body } = await get(\"/2020-01-02?to=USD\");\n  assert.ok(res.ok, `expected 2xx, got ${res.status}`);\n  assert.equal(body.date, \"2020-01-02\");\n});\n\ntest(\"an unknown currency is rejected with a non-2xx\", async () => {\n  const { res } = await get(\"/latest?from=ZZZ\");\n  assert.ok(!res.ok, `expected an error status, got ${res.status}`);\n  assert.ok(res.status >= 400 && res.status < 500);\n});\n\ntest(\"rates stay within a sane band (drift guard)\", async () => {\n  const { body } = await get(\"/latest?from=EUR\");\n  const values = Object.values(body.rates);\n  assert.ok(values.length > 5, \"expected a basket of rates\");\n  for (const rate of values) {\n    assert.ok(rate > 0 && rate < 100000, `rate out of band: ${rate}`);\n  }\n});\n"
    }
  ],
  "issue": {
    "title": "Add a contract test for the /currencies endpoint",
    "body": "Good first issue! Frankfurter has a `GET /currencies` endpoint that returns a flat object of `{ \"USD\": \"United States Dollar\", ... }`.\n\nAdd a test in `test/contract.test.js` that:\n1. Fetches `${API_BASE}/currencies`.\n2. Asserts the response is OK and the body is a non-empty object.\n3. Asserts every key matches `/^[A-Z]{3}$/` and every value is a non-empty string.\n4. Asserts `EUR` and `USD` are both present.\n\nRun `npm test` to check. Under an hour — comment to claim it!"
  }
}
